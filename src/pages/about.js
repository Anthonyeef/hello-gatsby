import React from "react"
import Header from '../components/header'

export default() => (
    <div style={{ color: `teal` }}>
        <Header headerTitle="About Gatisby"/>
        <p>So much, very React</p>
    </div>
)